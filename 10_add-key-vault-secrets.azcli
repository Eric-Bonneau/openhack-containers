az keyvault secret set --vault-name aks-secrets-team1 \
    --name SQL-DBNAME --value mydrivingDB

az keyvault secret set --vault-name aks-secrets-team1 \
    --name SQL-USER --value sqladminkTy1185

az keyvault secret set --vault-name aks-secrets-team1 \
    --name SQL-PASSWORD --value Password123

az keyvault secret set --vault-name aks-secrets-team1 \
    --name SQL-SERVER --value sqlserverkty1185.database.windows.net